##examples/for_seq.av

let m = seq.map("a",1,"b",2,"c",4);

for x in m{
    p(x.key + "=" + x.value);
}

let list = seq.list(1, 2, 3, 4, 5, 6, 7, 8, 9);

let sum = 0;
for x in list {
  sum = sum + x;
}

println("sum of list is "+ sum);